# Requirements for custom Double DQN training
# Note: gym-zombie-shooter must be installed separately first

# Core RL library (optional, only if using SB3)
stable-baselines3[extra]>=2.0.0

# Visualization
tensorboard>=2.12.0

# Optional: for better progress bars
tqdm>=4.65.0

# For memory profiling in custom agent
pympler>=1.0.1

# PyTorch (adjust for your CUDA version or use CPU)
# For CUDA 12.9, install separately: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu129
torch
torchvision
torchaudio
